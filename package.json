{
  "name": "swap-contract",
  "version": "0.1.0",
  "description": "Swap Contract",
  "private": true,
  "author": "Derrek Julian",
  "license": "ISC",
  "scripts": {
    "compile": "truffle compile",
    "migrate": "truffle migrate --network bsc --reset",
    "migrate:testnet": "truffle migrate --network testnet --reset --compile-none",
    "test": "truffle test"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "^4.4.0"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.4.1",
    "@openzeppelin/contracts-upgradeable": "^4.6.0",
    "@openzeppelin/truffle-upgrades": "^1.15.0",
    "@truffle/hdwallet-provider": "^2.0.0",
    "dotenv": "^10.0.0",
    "truffle": "^5.4.24",
    "truffle-assertions": "^0.9.2",
    "truffle-plugin-verify": "^0.5.20"
  }
}
